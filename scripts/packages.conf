# Package list consumed by scripts/pkg_install.sh
#
# Format:
#   name
#     -> same package name on every manager
#   name:brew:pacman:dnf:apt
#     -> per-manager override. Leave a field empty to fall back to `name`.
#
# Blank lines and lines starting with # are ignored.
 
curl

fd:fd:fd:fd-find:fd-find
ripgrep
fzf
yq
jq
zoxide

bat
git-delta
difftastic

just
make
cmake
